# zephyr/module.yaml
name: zmk-behavior-oneshot-layer # any name; must be unique
build:
  cmake: . # use the repo root CMakeLists.txt
  # optional Kconfig: not needed here
  settings:
    board_root: .
    dts_root: dts # so our bindings are picked up
